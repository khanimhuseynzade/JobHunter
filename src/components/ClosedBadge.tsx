export function ClosedBadge() {
  return (
    <span className="chip-closed inline-flex shrink-0 rounded-full px-2 py-0.5 text-[10px] font-medium leading-none">
      Possibly closed
    </span>
  );
}
